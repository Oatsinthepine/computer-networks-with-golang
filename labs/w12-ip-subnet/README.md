# W12 — IPv4, subnet and routing

## 练习

1. 分别计算 `/24`、`/26`、`/28` 的地址数量和可用 host 范围。
2. 查看本机接口与默认路由：`ifconfig`、`netstat -rn`。
3. 对一个公开域名运行 `traceroute`，区分本机、默认网关和运营商路径。

不要把本机公网 IP 或企业 VPN 路由原样提交到仓库；笔记中使用脱敏地址。
