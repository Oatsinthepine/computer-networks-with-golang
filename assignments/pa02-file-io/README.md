# PA2 — File I/O

完成日期：2026-08-22

本目录包含 PA2 讲义示例、最终 `PA2.go`、人工测试输入和运行结果。PA2 按“学习目标完成”口径记录为已完成。

## 知识点

文件 I/O、buffered I/O、逐行读取、EOF 与错误处理。

## 验收

- [x] 完成讲义中的 Hello World、标准 I/O、普通文件访问、buffer input 和 buffer output 示例。
- [x] 独立创建 `PA2.go`。
- [x] 使用普通文本、空文件、空行、含空格行和不存在文件进行验证。
- [x] 在 `evidence/README.md` 记录结果。

## 实现说明

官方要求是在每行原文前添加行号。当前实现另外输出了该行的字符数，格式为“行号 + 字符数 + 原文”。字符数列作为个人扩展保留，因此本次标记表示学习目标已经完成，不表示输出格式与官方参考程序严格一致。

当前未配置 Go `_test.go` 自动化测试；完成状态依据人工运行结果和独立编译验证。
