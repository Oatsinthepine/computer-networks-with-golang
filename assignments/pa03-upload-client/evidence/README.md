# PA3 evidence

## 本地手动流程的端到端验证

- 新增 `examples/upload_server/main.go`，监听 `127.0.0.1:12000`，接收一次后退出，不写磁盘。
- 使用 PA3 客户端上传 `testdata/upload.txt`，本次文件大小 64 字节。
- 客户端实际回复：`Server replies: 64 bytes received`；服务器打印了文件正文，双方正常退出。
- 此验证由 agent 运行，不代表用户已独立完成练习。

- 日期：2026-09-20
- 对端 server：自动化测试中的临时协议接收端，非学校服务器。
- 上传验证：普通文本（含空行）、空文件、二进制数据均逐字节比对通过；文件路径包含空格。
- 回复：客户端正确读取并打印 `upload received`。
- 使用的 TCP 地址：`127.0.0.1` 的系统分配临时端口。
- 验证命令：`go test -race ./assignments/pa03-upload-client`，通过。
- 完成范围：客户端实现和上述本地自动化验证已完成；尚未做用户手动端到端验证或学校服务器测试，不代表学习掌握状态。
- 测试环境：沙箱内首次运行出现标准库读取错误，获准在沙箱外重试后通过。
- 新手版复测：主要流程改为 main 中顺序执行，使用 ReadFile、WriteString、Write 和 Scanner；同步调整测试后，普通文本、空文件、二进制及含空格路径的 race 测试再次通过。移除了命令行参数及网络超时，采用整文件读入内存，仅用于小文件学习。
